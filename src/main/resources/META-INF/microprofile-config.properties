# Eventstream config defaults, shipped from this library jar. Library jars ship defaults as
# META-INF/microprofile-config.properties (ordinal 100) — a jar's application.properties is ignored
# — read by whichever app pulls the jar in (`ci` and the qits-projects `epics` module ship their
# config the same way). The consuming app (250) and the environment (300) override any of these.
#
# One property this module reads is deliberately NOT defaulted here: qits.environment, the tier
# stamped onto every published envelope. It is deployment-ambient — the MicroProfile spelling of
# the QITS_ENVIRONMENT the deployer injects into environment-tier services — and where nothing
# injects it the bus stamps the literal "platform" in code, so a default here would only shadow
# that rule. qits-integrations-quarkus' qits-environment-core reads the same property for the
# X-Qits-Environment header; the name is spelled in both repos (the extraction rule forbids the
# import), so grep both on a rename.

# ---------------------------------------------------------------------------------------------
# Where qits-events answers: scheme + host + port, NO PATH. The platform shape, the same one
# qits.ci.git-host-url and qits.cd.intake-url follow — the service is a deployment fact and the
# path is the client's knowledge. This module appends both of them itself: /events/api/events/{id}
# for the idempotent PUT and /events/stream for the websocket, with the scheme swapped to ws(s) for
# the second. Setting a path here yields a doubled one and a 404 nothing retries out of.
#
# The default is the qits-net alias, which is right for any deployment on that network and wrong
# for a host-run process, exactly like qits.artifacts.npm.* — a stack that publishes qits-events on
# a mapped localhost port overrides it.
qits.events.url=http://qits-events:8080

# The module's master switch, on by default because a library that ships dark is a library whose
# first deployment discovers it was never wired up. The DARKNESS BELONGS TO THE CONSUMER: the
# deployable's application.properties is where %dev and %test turn it off, the same policy OTel
# carries, so a `quarkus:dev` with no qits-events on the far side makes no dials rather than
# retries. Off means publish() is a debug log, the sweeper does nothing, and the subscriber never
# dials — there is no half-enabled state.
qits.eventstream.enabled=true

# How long the INLINE publish attempt may take before it is treated as "not now" and handed to the
# outbox. Short on purpose: the call sits on the publishing service's own thread — for qits-ci, the
# single-threaded run worker between one pipeline and the next — so this is the price every green
# build pays while qits-events is unreachable, and the outbox exists precisely so that price stays
# a few seconds instead of a retry budget's worth. The connect timeout is a separate two seconds,
# in EventsPublisher, where a static HttpClient could not go.
qits.eventstream.publish-timeout=PT5S

# The retry budget and how often the sweeper looks.
#
# THE BUDGET COUNTS REFUSALS ONLY — attempts that got an HTTP response saying no — and it counts the
# inline one, so five means the inline PUT plus four sweeps against a service that is answering,
# spaced 1s / 4s / 16s / 64s by RetrySchedule. AN ATTEMPT THAT GOT NO RESPONSE AT ALL SPENDS NOTHING:
# a connect failure or a timeout is rescheduled indefinitely, walking the same curve out to its
# five-minute cap. That split was bought on 2026-08-10, when a publisher aimed at an unresolvable
# alias spent its whole budget on ConnectExceptions and left FAILED rows nothing would retry.
#
# The spacing is held per row in next_attempt_at, so the cadence below is only a floor on how late a
# retry can be and never causes an early send. Raising max-attempts extends the schedule (1s · 4^n,
# capped at 5m) rather than repeating the last gap.
qits.eventstream.max-attempts=5
qits.eventstream.sweep-interval=10s

# The subscriber's redial backoff: doubled per consecutive failure from the first value, capped at
# the second. Gentler than the outbox's factor of four because the stream is live-only — what is
# broadcast during a disconnect is not replayed, so a long backoff loses events rather than merely
# delaying them, and the cost of coming back a little eagerly is one handshake.
qits.eventstream.redial-initial-backoff=PT1S
qits.eventstream.redial-max-backoff=PT30S

# ---------------------------------------------------------------------------------------------
# DURABLE CONSUMPTION. These four govern QitsDurableEventListener, and nothing else in the module
# reads them: an application with no durable listener writes no watermark, sweeps nothing and pays
# none of this.
#
# How often the catch-up sweep pages the log forward from each durable consumer's watermark. It also
# runs once at startup, which is the cutover case — a consumer that was down or being replaced while
# an event was broadcast reads it back here rather than losing it. The interval is therefore the
# WORST-CASE lateness of an event the stream did not deliver, not of an ordinary one: a live frame
# still arrives live, and the sweep only finds what the socket missed.
qits.eventstream.catchup-interval=PT30S

# How long one sweep may hold the catch-up lock before the watchdog calls it stalled.
#
# BOUGHT ON 2026-09-08, BY SIX HOURS OF GREEN-WHILE-DEAD. qits-deployments self-deployed at
# 00:10:33; its eventstream-catchup-startup thread entered catchUp(), went down through
# ConsumerWatermarkRepository.findById into Agroal's borrow validation, and parked forever in a
# PostgreSQL socket read — a half-open TCP connection left by the blue-green cutover overlap, with
# no JDBC socket timeout to end it. Narayana's reaper aborted the transaction at 00:11:45
# (ARJUNA012117/012095/012381) and the thread did not move: a reaper abort does not unpark a socket
# read. catchUp() was `synchronized` and the scheduled tick runs SKIP, so that one parked thread
# held the monitor until 06:30. Zero events consumed for six hours, every release of that window
# silently undeployed, every health check green. Only a process restart healed it.
#
# FIVE MINUTES IS DELIBERATELY GENEROUS, because a long sweep is a NORMAL thing: a consumer that has
# been down for a day pages a day of backlog through its handler, and killing that would turn a
# recovery into a loop of recoveries. The budget is therefore sized as "no honest sweep runs this
# long", not as "a sweep should be quick" — it is an order of magnitude above the PT30S interval.
#
# WHEN IT EXPIRES the tick that found the lock held logs an ERROR naming the holder and its thread,
# interrupts that thread, and returns without sweeping. On a VIRTUAL thread the interrupt frees a
# blocking socket read (measured on Temurin 25.0.4.1: unblocked in 1ms with
# java.net.SocketException: Closed by interrupt), which is exactly what the incident's stalled
# thread was. On a PLATFORM thread it does not, so for a wedged tick the ERROR and the
# `eventstream-catchup` readiness check going DOWN are the whole cure — the signal that was missing.
# The complementary real fix is a JDBC socketTimeout on the datasource, which is not shipped here.
qits.eventstream.sweep-stall-budget=PT5M

# Whether the startup sweep happens at all. On, because a start is exactly when a consumer is behind.
# It runs on its own thread, so it never delays boot and never fails one. A TEST SUITE TURNS THIS OFF
# — a sweep landing behind a test is the non-determinism quarkus.scheduler.enabled=false already
# keeps out — and drives CatchupSweeper#catchUp by hand instead.
qits.eventstream.catchup-at-startup=true

# How far below the watermark a handled-event claim is kept before it is pruned. Once the watermark
# has passed an event, catch-up will never offer it again and the claim protects against nothing, so
# this is pure overlap: cover for the window in which the stream has delivered something the sweep
# has not yet read past. A DAY IS DELIBERATELY GENEROUS, because the comparison mixes two clocks —
# the claim's own (this consumer's) against a bound derived from the event's (the publisher's) — and
# a horizon of minutes would make host clock skew load-bearing.
qits.eventstream.prune-horizon=P1D

# ---------------------------------------------------------------------------------------------
# This module owns its OWN named datasource + persistence unit + Flyway lineage: its own postgres
# database, never sharing the consuming service's database or its migration history. That is the
# `ci` and `epics` shape, and here it is load-bearing twice over — the outbox must survive the
# process, and this module already left the repository it grew in, which the split is what made
# cheap.
#
# THE THREE KEYS BELOW ARE THE PLATFORM'S GENERIC RESOURCE CONTRACT, read back by the jar that
# needs it. A consuming repository declares `resources: postgresql:eventstream:<database>` in its
# .config/qits/deployments.yml; qits-deployments creates the role and the database before the
# cutover and injects QITS_RESOURCE_EVENTSTREAM_URL / _USERNAME / _PASSWORD into the container it
# starts. Nothing in the deployer is eventstream-specific — an application maps the generic
# variables in its own shipped defaults, and this file is where this library does it. The variable
# names follow the resource NAME, so a consumer that renames the resource stops matching this file:
# the name is `eventstream`, in every consumer.
#
# THE TRIPLE HAS NO DEFAULTS AND THAT IS THE REFUSE-TO-BOOT STANCE. An unset variable leaves these
# expressions unresolvable, so the process dies at Flyway's migrate-at-start naming what is missing,
# rather than opening some fallback store nobody meant. There is no local file to fall back to any
# more — and that is the end of the AUTO_SERVER-shaped class of bug this jar cost a rollout over
# once: the shipped default carried a `${user.home}` a container has no value for, and only the
# packaged artifact in its real environment ever found out. A default with no feature in it cannot
# lose one.
quarkus.datasource.eventstream.db-kind=postgresql
quarkus.datasource.eventstream.jdbc.url=${QITS_RESOURCE_EVENTSTREAM_URL}
quarkus.datasource.eventstream.username=${QITS_RESOURCE_EVENTSTREAM_USERNAME}
quarkus.datasource.eventstream.password=${QITS_RESOURCE_EVENTSTREAM_PASSWORD}

# THE POOL SURVIVES A POSTGRES CUTOVER, WHICH STOCK AGROAL DOES NOT. Replacing the tier's postgres
# severs every pooled connection; without validation the pool hands a dead one to the next caller
# long after the database is back, and 5s of acquisition is not enough for the successor to take the
# alias. Validate at borrow, and wait 15s before giving up. These three ship at ordinal 100, so
# every consumer's outbox and claim ledger get them without restating a line — and a consumer that
# means something else overrides any of them at 250, like every other default in this file.
#
# The driver line is the one that makes a request WAIT instead of fail: PatientPgDriver delegates to
# org.postgresql.Driver and, while the database is not there, keeps asking for up to 14s — under the
# 15s above, so a caller that outlasts it gets the database's real refusal rather than a generic
# acquisition timeout. It retries only "not there" (SQLState 08*, and 57P03 while postgres is
# starting up); a wrong password fails at once. NAMING IT HERE IS WHY THIS JAR DEPENDS ON
# qits-db-core AT RUNTIME: a default that names a class every consumer must have is only a default
# if the jar brings the class with it.
quarkus.datasource.eventstream.jdbc.driver=eu.wohlben.qits.db.PatientPgDriver
quarkus.datasource.eventstream.jdbc.acquisition-timeout=15S
quarkus.datasource.eventstream.jdbc.validate-on-borrow=true

# A dedicated persistence unit bound to that datasource, owning only this module's entity. packages
# assigns entity→PU unambiguously, which is what keeps the outbox out of the consuming service's
# default persistence unit.
quarkus.hibernate-orm.eventstream.datasource=eventstream
quarkus.hibernate-orm.eventstream.packages=eu.wohlben.qits.eventstream.entity

# Own Flyway lineage at a distinct classpath location so it never mixes with a consumer's. ONE
# lineage, starting at V1: the move to postgres was a clean start with no history inherited from the
# H2 files (see V1__init.sql's header for why that was allowed) — and from V1 onward the ordinary
# rule is back, so V2 and V3 append rather than edit.
#
# baseline-on-migrate is GONE with that lineage. It told Flyway to adopt a non-empty schema it had
# no history table for, which the H2 file needed while it was being adopted; every database this
# code now meets is empty or was created by this very lineage, and the flag would only mask the one
# case worth failing on — a schema somebody else put there.
quarkus.flyway.eventstream.migrate-at-start=true
quarkus.flyway.eventstream.locations=classpath:db/eventstream/migration
